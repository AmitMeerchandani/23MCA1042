// Pagination.js
import React from "react";

const Pagination = ({ currentPage, totalPages, onPageChange }) => {
  // Implement your pagination UI and logic here
  return <div className="pagination">Pagination</div>;
};

export default Pagination;
